# Data Registry

This directory contains **controlled registries** such as:
- SOURCE_REGISTRY.csv
- FACTOR_FILES_MANIFEST.csv

These registries enforce source integrity and prevent hallucinated or
unverifiable data sources.
