# Derived Subsets

This directory contains **derived, non-authoritative subsets** of frozen datasets
(e.g., state-wise extracts).

Rules:
- Subsets MUST be reproducible from data/frozen/
- Subsets MUST NOT be cited as authoritative
- Frozen datasets remain the single source of truth
